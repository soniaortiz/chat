{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "outDir": "../../dist",                        /* Redirect output structure to the directory. */
         "rootDir": ".",
         "module": "commonjs" ,
    },
    "exclude": [
        "node_modules"
    ]
}